package MainPackage.repository;


//import MainPackage.model.Slides;
import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

//@Repository
import org.springframework.data.jpa.repository.JpaRepository;
import java.util.List;

//public interface SlideRepository extends JpaRepository<Slides, Long> {
 //   List<Slides> findByUserId(Long userId); // Find all slides uploaded by a specific user
//}
